@model IEnumerable<SfiziAmerica.EntityLayer.Model.Rezervation>
@{
    ViewData["Title"] = "Rezervation List";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
@section scripts{
    <script src="/PageJs/rezervation.js"></script>
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="mt-0 header-title">Rezervation List</h4>

                <table id="datatable-buttons" class="table table-striped table-bordered dt-responsive nowrap">
                    <thead>
                        <tr>
                            <th>Date : Time</th>
                            <th>Name Surname</th>
                            <th>Phone</th>
                            <th>Email</th>
                            <th>Persons</th>
                            <th>Table Type</th>
                            <th>Status</th>
                            <th></th>
                        </tr>
                    </thead>


                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Date | @item.Time</td>
                                <td>@item.NameSurname</td>
                                <td>@item.Phone</td>
                                <td>@item.Email</td>
                                <td>@item.Person</td>
                                <td>@item.TableType</td>
                                @if (item.IsActive)
                                {
                                    <td>Active</td>
                                }
                                else
                                {
                                    <td>Passive</td>
                                }
                                <td>
                                    <a href="/admin/rezervasyon-guncelle/@item.ID" class="btn btn-sm btn-primary"><i class='fa fa-edit'></i></a>
                                    <button data-id="@item.ID" class="btn btn-danger btn-sm btnDelete"><i class='fa fa-trash'></i></button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</div>
<!-- end row -->